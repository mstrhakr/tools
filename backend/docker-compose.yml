services:
  api:
    build: .
    image: mstrhakr/tools-api:latest
    container_name: tools-api
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:8080"
    environment:
      - PORT=8080
    # NET_ADMIN only needed if you later add raw ICMP ping
    # cap_add:
    #   - NET_ADMIN
